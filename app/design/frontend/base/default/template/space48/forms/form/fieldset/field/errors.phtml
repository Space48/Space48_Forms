<?php if ( $errors = $this->getErrors() ) : ?>
    <ul class="errors">
        <?php foreach ( $errors as $error ) : ?>
            <li>
                <span>
                    <?php echo $error; ?>
                </span>
            </li>
        <?php endforeach; ?>
    </ul>
<?php endif; ?>
